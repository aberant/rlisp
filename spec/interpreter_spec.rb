require "spec_helper"

describe Interpreter do
  before(:each) do
    @interpreter = Interpreter.new
  end
  
  # it "should be able to add numbers" do
  #   @interpreter.eval("(+ 1 2)").should eql("3")
  # end
end
